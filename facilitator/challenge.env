# Citadelle escape-game content source
# Values use JSON-style quotes so punctuation and spaces remain safe.
# Run `npm run generate:challenges` after editing.

IMAGE_FLAG="FLAG{IMAGE_FORENSICS_COMPLETE}"
IMAGE_CHARADE="My first is a precious metal whose name also describes a pale, shining colour."
IMAGE_NEXT="Next evidence: network_capture.log"
IMAGE_SOURCE="facilitator/assets/nyc.original.jpeg"
IMAGE_OUTPUT="public/img/nyc.jpeg"

DNS_FLAG="FLAG{DNS_TUNNEL_FOUND}"
DNS_CHARADE="My second flows between two banks but is not money."
DNS_NEXT="Next destination: the public Citadelle website."
DNS_DOMAIN="exfil.training.local"

WEBSITE_FLAG="FLAG{SOURCE_CODE_INSPECTED}"
WEBSITE_CHARADE="My third is the answer to six multiplied by seven."
WEBSITE_NEXT="Next file: fraud_analysis.ipynb"

NOTEBOOK_FLAG="FLAG{FRAUD_MODEL_VERIFIED}"
NOTEBOOK_CHARADE="My fourth tells you to write the words together, capitalising the start of each word, then add the number."
NOTEBOOK_NEXT="My whole is the new administrator password."

# Optional Help Centre prompt-injection challenge. This bridges students toward
# the website-source investigation without replacing a charade component.
CHAT_FLAG="FLAG{HELP_ASSISTANT_BYPASSED}"
CHAT_SECRET_NOTE="Internal case HC-204: Inspect the regional locations page source. The useful values are split across an office ledger, an archive note, and retired display preferences."
CHAT_NEXT="Next destination: Citadelle locations."

# Facilitator-only answer. The generated website receives only its SHA-256 hash.
FINAL_PASSWORD="SilverRiver42"
GENERATION_SEED="citadelle-2026"
